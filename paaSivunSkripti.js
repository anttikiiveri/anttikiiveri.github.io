function aloitaTesti(tiedosto) {
    console.log('Aloita testi:', tiedosto);
    // Lisää haluamasi toiminnallisuus tänne
    // Voit esimerkiksi ladata toisen sivun käyttäen window.location.href
    // tai tehdä muita toimintoja tässä vaiheessa
}
